<!DOCTYPE html>
<html>
<head>
    <title>DZB eReader Demo</title>
    <link rel='stylesheet' href='/stylesheets/bootstrap-theme.css'/>
    <link rel='stylesheet' href='/stylesheets/bootstrap.css'/>
    <link rel='stylesheet' href='/stylesheets/eReader_login.css'/>
</head>
<body>

<script>
    setTimeout(function sunrise() {
        document.getElementsByClassName('header')[0].style.backgroundColor = '#303d6c';
    }, 0);
</script>

<header>
    <div class="row">
        <div class="col-md-8 col-md-offset-1">
            <h1 id="main-title" class="container">DZB eReader 0.1.0</h1>

            <h3 class="container">Barrierefreier E-Book-Reader</h3>
        </div>
        <div id="logo" class="col-md-3">
            <a href="http://www.dzb.de/" title="DZB Hauptseite"><span class="invisible">DZB Hauptseite</span></a>
        </div>
    </div>
</header>

<section class="main container">
    <form id="login"
          class="form-horizontal col-md-6 border"
          action="/login"
          method="POST"
          aria-describedby="loginFailed"
          role=form>

        <!--ARIA-->
             <span id="loginFailed"
                   role="tooltip"
                   class="tooltip <% if(JSON.stringify(message[0]) == undefined) { %>hidden<% } %>"
                   aria-hidden= <%if(JSON.stringify(message[0]) == undefined) { %>"true"<% } else { %>"false"<% } %>>
        Ihr Login ist fehlgeschlagen (<%=JSON.stringify(message[0])%>)</span>

        <div class="form-group">
            <label for="email" class="control-label col-xs-2">E-Mail</label>
            <div class="col-xs-10">
                <input name="username"
                       type="email"
                       class="form-control"
                       id="email"
                       placeholder="E-Mail"
                       role=textbox
                       aria-describedby="username-tip"
                       required>
                <div role="tooltip" id="username-tip">Ihr Nutzername ist Ihre E-Mail-Adresse</div>
            </div>
        </div>
        <div class="form-group">
            <label for="password" class="control-label col-xs-2">Passwort</label>
            <div class="col-xs-10">
                <input name="password"
                       type="password"
                       class="form-control"
                       id="password"
                       placeholder="Passwort"
                       role=textbox
                       aria-describedby="password-tip"
                       required>
                <div role="tooltip" id="password-tip">Das Passwort wurde Ihnen zugschickt.</div>
            </div>
        </div>
        <div class="form-group">
            <div class="col-xs-offset-2 col-xs-10">
                <button type="submit"
                        value="submit"
                        class="btn btn-primary"
                        role=button
                        aria-controls="email"
                        >Login</button>
                <small>
                    <a href="" role=link>Sie haben Ihr Passwort vergessen?</a>
                </small>
            </div>
        </div>
    </form>
</section>
<script src="javascripts/jquery.min.js"></script>
<script src="javascripts/login.js"></script>
</body>
</html>